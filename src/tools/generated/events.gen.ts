// Auto-generated by scripts/generate-tools.ts — DO NOT EDIT
import { z } from 'zod'
import type { ToolRegistry } from '../../registry.js'
import type { AuthAdapter } from '../../auth-adapter.js'

export function registerEventsTools(registry: ToolRegistry) {
  registry.register({
    name: "bulk_import_events",
    description: `Bulk import engagement events

Imports up to 5000 engagement events in a single request. Resolves engagements by lead sale ID (type=lead) or caller phone (type=call). Processes in batches with idempotency checks. Normalizes statuses via intake center mappings and updates engagement terminal/CPA status.`,
    zodSchema: z.object({
    events: z.array(z.object({ type: z.enum(['lead', 'call']), identifier: z.string().describe('leadSaleId for type=lead, callerPhone for type=call'), contractId: z.string(), status: z.string(), disposition: z.string().optional(), payout: z.string().optional(), occurredAt: z.string().optional() })),
  }),
    handler: async (args: Record<string, unknown>, auth: AuthAdapter) => {
      const resolvedPath = '/api/admin/events/bulk'
      const body = args
      return auth.fetch(resolvedPath, { method: 'POST', body })
    },
    metadata: {domain:"events",entity:"import",isWrite:true,confirmRequired:true,method:"POST",path:"/api/admin/events/bulk",tags:["events"],summary:"Bulk import engagement events",allowedRoles:["admin","superadmin"],annotations:{}},
  })

}